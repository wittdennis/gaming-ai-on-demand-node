---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
buildMetadata: [originAnnotations]
# One line per component. Kustomize recurses into each directory's own kustomization.yaml.
# external-secrets/ is deliberately absent: it is an earlier layer this one depends on.
resources:
  - rbac
  - node-feature-discovery
  - amd-device-plugin
  - mogenius-operator
